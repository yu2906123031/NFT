# 双击启动

1. 双击 `01-PREFLIGHT.bat`：测试 Node 环境、启动时间窗口、运行锁、离线回归、程序语法、所有读取节点、所有广播节点、六钱包余额/nonce/资格/费用预算、开售时间模拟及离线签名。不会广播有效交易。结束时打印汇总，按键关闭窗口。
2. 检查 `reports` 中的时间戳 TXT 报告及 JSON 汇总；部分预热连接超时会在 TXT 中标为 WARNING。PASS 仅代表本次检查通过，不保证开售时仍有余量或一定入块。
3. 至少提前两分钟双击 `02-LIVE-MINT.bat`。这是实盘启动入口，会直接启动正式多钱包程序。看到 ARMED 后保持窗口打开，不休眠，不要再次使用这些钱包发交易。运行日志也保存到 reports。

当前方案：2026-09-16 北京时间 22:00:20 开售；wallet1–wallet3 按本机时钟，wallet4–wallet6 等链上开售区块；每钱包 Mint 1 个，八条 RPC 广播同一笔签名交易。首次明确回滚后，只有仍有余量、资格正常、当前模拟通过且预算足够时最多重试一次。pending/回执未知不重试。

两个 BAT 都会切换到自身目录。不要同时打开多个实盘窗口。测试报告不会替代实盘启动，也不会自动启动实盘。已运行的旧进程不会自动加载修改。